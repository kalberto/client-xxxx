.logo
    background url('../img/logo.png') no-repeat center center
    background-size: contain;
    // background-color #40818A
    // -webkit-mask:  url('../img/logo.svg') no-repeat center center;
    // mask: url('../img/logo.svg') no-repeat center center;
    display flex
    align-items center
    justify-content center
    width 100%
    height 60px
    z-index 2000
    margin 0 auto
    padding 0

    &.white
        fill #fff
        background-color #535a5a
        background url('../img/logo.png') no-repeat center center
        background-size: contain;
